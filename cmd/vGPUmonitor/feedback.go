package main

func watchAndFeedback() {
	/*
		time.Sleep(time.Second)
			srlist, err := monitorpath()
			if err != nil {
				for idx, val := range srlist {
					if val.sr.priority == 0 {
						srlist[idx].sr.recentKernel++
					}

				}
			}
	*/
}
